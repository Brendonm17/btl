import "system";

func isEven(n) {
  if (n == 0) return true;
  return isOdd(n - 1);
}

func isOdd(n) {
  if (n == 0) return false;
  return isEven(n - 1);
}

system.println(isEven(4)); // expect: true
system.println(isOdd(4));  // expect: false