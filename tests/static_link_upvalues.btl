// Test Static Link Elimination (Flattening)
// Even though 'a' is 3 levels up, the innermost function 
// should access it via a direct index in its own closure.
func outer() {
  var a = "outer-value";
  func middle() {
    func inner() {
      return a;
    }
    return inner;
  }
  return middle;
}

var closure = outer()();
print closure(); // expect: outer-value