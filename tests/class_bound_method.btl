import "system";

class Person {
  func init(name) { this.name = name; }
  func sayName() { system.println(this.name); }
}

var p = Person("Alice");
var method = p.sayName; 
method(); // expect: Alice